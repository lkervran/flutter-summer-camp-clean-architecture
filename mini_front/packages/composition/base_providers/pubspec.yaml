name: base_providers
description: Composition — app-wide configuration + the provider-overrides assembly (start providers).
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  cabins_data:
  riverpod: ^3.2.1
  riverpod_annotation: ^4.0.2

dev_dependencies:
  build_runner: ^2.11.1
  riverpod_generator: ^4.0.3
