/// Cabins feature — presentation layer (public API).
///
/// Exposes the screen and the routing (the generated `CabinsRoute` the app
/// composes into its root router).
library;

export 'src/cabins_screen.dart';
export 'src/routing/cabins_router.dart';
