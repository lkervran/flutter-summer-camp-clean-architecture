name: givn
description: In-house Given/When/Then test framework (vendored into the camp from flutter-front).
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.6
  riverpod_annotation: ^4.0.2
